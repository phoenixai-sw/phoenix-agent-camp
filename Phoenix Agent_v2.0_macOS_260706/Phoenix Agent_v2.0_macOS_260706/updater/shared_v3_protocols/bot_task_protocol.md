# Phoenix Agent v2.0 Bot Task Protocol

Genesis Bot creates structured tasks instead of vague natural-language handoffs.

Never include API keys, Telegram tokens, OAuth credentials, raw `.env` values, or private chat ids in task payloads.
